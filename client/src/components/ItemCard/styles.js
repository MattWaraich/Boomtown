import color from "@material-ui/core/colors/teal";

const styles = theme => {
  return {
    cardItemImages: {
      paddingTop: "55%"
    },
    gravatarDisplay: {
      display: "flex",
      flexDirection: "row",
      paddingBottom: "80px"
    },
    gravatarName: {
      paddingLeft: "20px"
    },
    itemBorrowButton: {
      backgroundColor: "#fafafa"
    }
  };
};

export default styles;
